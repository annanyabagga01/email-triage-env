[project]
name = "email-triage-env"
version = "0.1.0"
description = "AI email triage environment"
authors = [
  { name="Annanya Bagga & Sohini Dutta" }
]
requires-python = ">=3.10"

dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic"
]
