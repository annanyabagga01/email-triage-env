name: email-triage-env

endpoints:
  reset: /reset
  step: /step
  state: /state

method:
  reset: POST
  step: POST
  state: GET

observation_space:
  email_text: string
  sender: string
  subject: string
  task_type: string

action_space:
  action_type: string
  value: string
